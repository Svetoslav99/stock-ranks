/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['d235dzzkn2ryki.cloudfront.net', 'images.moneycontrol.com', 'images.mktw.net', 's.marketwatch.com', 'images.barrons.com']
  }
};

module.exports = nextConfig;
